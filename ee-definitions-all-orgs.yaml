---
# AWX Execution Environment Definitions for All Organizations
# Auto-generated by generate_ee_for_all_orgs.sh
# Usage: source awx.conf && sudo -E ./manage_awx_ee.sh --build-all --definitions THIS_FILE

defaults:
  container_runtime: podman
  load_to_k3s: true
  register_in_awx: true

execution_environments:
  
  - name: "AWX EE Collections - BLR"
    description: "EE with common Ansible collections for BLR"
    ee_file: "examples/ee-collections.yml"
    image_tag: "awx-ee-collections"
    organization: 2
    enabled: true

  - name: "AWX EE Collections - HYD"
    description: "EE with common Ansible collections for HYD"
    ee_file: "examples/ee-collections.yml"
    image_tag: "awx-ee-collections"
    organization: 3
    enabled: true
  
